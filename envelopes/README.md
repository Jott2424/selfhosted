# Envelopes
### My own envelope-budgeting app

A small self-built Python app implementing envelope budgeting. Runs its own DB init on startup (`init-db`) and connects to the shared `p_postgres01` instance from [databases](../databases).
