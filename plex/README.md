# Plex
### Media server for movies, shows, and home videos

Runs with `network_mode: host` (recommended by Plex for DLNA and remote access to work properly). Libraries are split into separate mounts per media type/audience (adult vs. kids shows/movies, home videos, family movies), all served from `/mnt/Vault/Media` on the Truenas box.
