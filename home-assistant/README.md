# Home Assistant

### Home automation platform

Runs with `network_mode: host` and `privileged: true`, needed for device discovery (Bluetooth, mDNS, USB Zigbee/Z-Wave dongles, etc.) to work properly. Config persists under `/mnt/Vault/Homelab/Portainer/Stacks/HomeAssistant/dev/config` on the Truenas box.
