# Datalake (prod)
### The production side of the datalake — just MinIO for now

Everything else ([Unity Catalog](../dev/infra/UnityCatalog), [Spark](../dev/spark)) is still living in [`dev/`](../dev) until it's stable enough to promote here.
